# PartWorks-BE

Quick start

1. Clone the repository and go to the project directory:

```bash
git clone <repo-url>
cd PartWorks-BE
```

2. Download Go modules:

```bash
go mod download
```

3. Run the project:

```bash
go run ./cmd/api
```